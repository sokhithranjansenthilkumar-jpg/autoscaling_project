{
  "name": "autoscaling-node-helper",
  "version": "1.0.0",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "health": "node -e \"fetch('http://127.0.0.1:5000/health').then((res)=>res.text()).then(console.log).catch((err)=>{console.error(err.message);process.exit(1);})\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
